.zsh_plugins.zsh
env/secret.zsh
.DS_Store

# Generated by Noctalia's `hyprland` colour template from the active palette.
# Machine state, not config: hyprland.lua reads its colours table (see the
# comment there) and it is rewritten on every palette change.
config/hypr/noctalia.lua
